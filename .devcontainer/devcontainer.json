{
  "name": "Nimbus Workshop",
  "image": "mcr.microsoft.com/devcontainers/python:3.11-bookworm",

  "hostRequirements": { "cpus": 2, "memory": "8gb" },

  "// setup": "Everything expensive happens at image build time, so the room is not",
  "// setup2": "waiting on pip or on a model download during the activity.",
  "onCreateCommand": "make setup && .venv/bin/python .devcontainer/prefetch.py",

  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": { "label": "Nimbus", "onAutoForward": "notify" }
  },

  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"],
      "settings": { "python.defaultInterpreterPath": ".venv/bin/python" }
    }
  },

  "postAttachCommand": "echo '\\n  Nimbus workshop ready.\\n  Read README.md, then: make serve\\n'"
}
